# his041 — 옵션 패널 구현

**날짜**: 2026-04-26  
**브랜치**: main

---

## 작업 개요

SampleScene에서 ESC 키로 열리는 독립 옵션 패널 구현.  
하네스 엔지니어링 패턴(스프린트 계약 + QA 에이전트 게이팅) 적용.

---

## 완료 작업

### Sprint 0 — 기반 데이터

**StringTable.csv** — str.ui.option.* 키 9개 × 7언어 추가:
title / bgm_off / sfx_off / restart / quit / confirm_restart / confirm_quit / yes / no

**BGMManager.cs** — `_muted` 필드 + `Mute(bool)` 추가:
- PlayBGM()에서 `_muted ? 0f : targetVol` 체크
- SetVolume() _muted 가드 추가

**SFXManager.cs** — `_muted` 플래그 + `Mute(bool)` 추가:
- PlayClick() / PlaySFX() 진입부 `if (_muted) return;` 가드

**QA PASS** — TC-S0-01(StringTable 9키) TC-S0-02(BGMMute) TC-S0-03(SFXMute) 모두 통과

---

### Sprint 1 — C# 스크립트

**OptionPanel.cs** (신규):
- Toggle bgmToggle/sfxToggle — PlayerPrefs(opt_bgm_muted/opt_sfx_muted) 영속
- Button restartButton/quitButton/closeButton
- ConfirmPopup 연결
- `Show()` / `Hide()` 공개 API
- `RefreshLocalization()` — Loc.Get("str.ui.option.*")

**ConfirmPopup.cs** (신규):
- `Show(string message, Action onConfirm)`
- 예/아니오 버튼

**GameSettings.cs** — `public GameObject optionPanelPrefab` 필드 추가

**QA PASS** — TC-S1-01(OptionPanel 구조) TC-S1-02(ConfirmPopup) TC-S1-03(GameSettings) TC-S1-04(using) 모두 통과

---

### Sprint 2 — 프리팹 생성

**OptionPanelPrefabCreator.cs** (신규, Editor):
- 메뉴 `DopamineRace > Create Option Panel Prefab`
- MkChild/MkTextObj/MkToggle/MkButton 헬퍼
- Reflection으로 SerializeField 자동 연결
- GameSettings.optionPanelPrefab 자동 연결

**OptionPanel.prefab** 생성 (`Assets/Prefabs/UI/OptionPanel.prefab`):
- Toggle 2개, Button 5개, ConfirmPopup 자식 포함
- 완전히 독립 프리팹 (BettingPanel과 별개)

**QA PASS** — TC-S2-01(파일 존재) TC-S2-02(OptionPanel 컴포넌트) TC-S2-03(ConfirmPopup 자식) TC-S2-04(Toggle:2 Button:5) TC-S2-05(GameSettings 연결) 모두 통과

---

### Sprint 3 — SceneBootstrapper 연결

**SceneBootstrapper.cs**:
- `private OptionPanel _optionPanel;` 필드 추가
- `Awake()` 말미: `Instantiate(GameSettings.Instance.optionPanelPrefab)` + null 가드
- `Update()` 상단: `Input.GetKeyDown(KeyCode.Escape)` → Show/Hide 토글

**QA PASS** — TC-S3-01(컴파일) TC-S3-02(ESC 연결) TC-S3-03(OptionPanel 완전성) TC-S3-04(회귀 테스트) 모두 통과

---

## 검증 결과

- ✅ 컴파일 에러 0건
- ✅ Sprint 0~3 QA 게이트 전부 PASS (qa 에이전트 + MCP 자동 검증)
- ✅ OptionPanel.prefab 존재, OptionPanel 컴포넌트 연결 확인
- ✅ GameSettings.optionPanelPrefab 자동 연결 완료
- ✅ 기존 레이싱/배팅 기능 회귀 없음

---

## 수정 파일 목록

| 파일 | 변경 |
|------|------|
| `Assets/Resources/Data/StringTable.csv` | str.ui.option.* 9개 추가 |
| `Assets/Scripts/Manager/Audio/BGMManager.cs` | Mute(bool) 추가 |
| `Assets/Scripts/Manager/Audio/SFXManager.cs` | Mute(bool) + 재생 가드 추가 |
| `Assets/Scripts/Manager/UI/OptionPanel.cs` | 신규 |
| `Assets/Scripts/Manager/UI/ConfirmPopup.cs` | 신규 |
| `Assets/Scripts/Data/GameSettings.cs` | optionPanelPrefab 필드 추가 |
| `Assets/Scripts/Editor/OptionPanelPrefabCreator.cs` | 신규 |
| `Assets/Prefabs/UI/OptionPanel.prefab` | 신규 생성 |
| `Assets/Scripts/Manager/UI/SceneBootstrapper.cs` | ESC 키 + OptionPanel 인스턴스화 |
| `Docs/specs/spec024_옵션패널_20260426.md` | 신규 |

---

## 비고 (Inspector 수동 작업)

GameSettings.asset Inspector에서 `optionPanelPrefab` 슬롯이 이미 자동 연결됨  
(OptionPanelPrefabCreator가 EditorUtility.SetDirty + SaveAssets로 처리).

Play 모드 테스트 시:
1. SampleScene 진입
2. ESC 키 → 옵션 패널 열림 확인
3. BGM 토글 → 음악 무음 확인
4. 재시작 → 확인 팝업 → 예 → 게임 리셋 확인
