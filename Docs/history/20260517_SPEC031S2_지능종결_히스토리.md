# 20260517 SPEC-031 S2 — 지능 경량 종결 세션 히스토리

**날짜**: 2026-05-17 | **관련 SPEC**: spec031 (S2 절) | **방식**: 경량 재검증+동결

## 배경

SPEC-031 Phase1(지능 함정수정) 커밋 후, 원 후속 #2 "S2 풀 스윕"이 SPEC-032
타입 매크로 전면 재튜닝으로 전제 무효화. 조사로 확인:
- `v4_intBurstShift/DrainFloor` asset 미직렬화(코드기본값 0.5/0.55만)
- `equalStats`가 v4Intelligence를 상수화 → 지능효과 측정 불가
- equalStats OFF는 개별캐릭 불균형(고요 73~86%)이 승률 지배 → 미세 스윕 무의미
- 타입 표기 설명 UI: 도주/선행/선입/추입은 타깃 유저 상식 → 오너 "설명 불필요" 폐기

→ 오너 결정: **경량 — 재검증 + 값 명시 동결** (정밀 스윕은 개별캐릭 SPEC 이후).

## 작업 / 결과

1. **직렬화**: AnkleBreaker MCP로 GameSettingsV4 에셋 로드 → 두 필드 0.5/0.55 set →
   SetDirty+SaveAssets+ImportAsset → asset YAML L58-59 영속 확인(grep). 코드 0변경
2. **결정적 재검증**(MCP, asset 실값): Runner 0.08–0.36·burstDrainMul3·floor0.55 →
   INT20 drain 20.8(×1.65) shift+0.028 / INT10·0 37.8(×3.0). 고INT<저INT 단조 유지
   = 함정 역전 새 매크로서도 보존 ✅
3. **타입 σ 회귀 가드**(실스탯 300/트랙): σ=2.67 (SPEC-032 1.98~3.81 범위, ≤5)
   → 직렬화가 타입 밸런스 불교란 ✅
4. 문서: spec031 "S2 경량 종결" 절 추가 + 본 히스토리. 커밋·푸시

## 환경 메모

- Signal Loop MCP 끊김 지속 → AnkleBreaker `unity_execute_code` 사용
  (메서드 본문 래핑: using 금지·정규화 타입명)
- 백테스트 로그 .gitignore → 커밋 제외, 파일 직접 grep으로 집계

## 변경 파일

- `Assets/Resources/GameSettingsV4.asset` (2필드 직렬화, 값 불변)
- `Docs/specs/spec031_지능부스트함정해소_20260516.md` (S2 절 추가)
- 코드 3파일·프리팹·시뮬 = 불변

## 미결 / 후속

- 개별 캐릭터 스탯 밸런스 신규 SPEC (고요 독식·일부 0% 해소) → 이후 정밀 지능 스윕
- 부수(비차단): str.chartype.reckoner 영어 'Nachzügler' 오입력 → 'Closer' 정정 권고
- 커밋 `[C] SPEC-031 S2 경량 종결 — 지능 필드 직렬화+재검증`
