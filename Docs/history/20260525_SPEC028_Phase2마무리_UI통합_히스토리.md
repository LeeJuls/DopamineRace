# SPEC-028 Phase 2 마무리 + UI 통합/정리 히스토리 (2026-05-25)

## 개요
SPEC-028 Phase 2 (베팅액 모달 + 통화 헤더 + 환전 시스템) 잔여 작업 완료, 그리고 BettingPanel 중심으로 분산돼 있던 UI 요소들을 통합 + 비주얼 일관성 확보.

---

## 작업 결과 (커밋 시간순)

### 프리팹 폴더 정리 + 단일 사용처 통합
| 커밋 | 내용 |
|------|------|
| `fcf460a` | SPEC-028 프리팹 4개를 `Resources/Prefabs/UI/` → `Prefabs/UI/`로 이동, GameSettings 직렬화 필드 방식으로 통일 |
| `845bc89` | **ExchangeIcon → BettingPanel** 자식으로 통합 (단일 아이콘 별도 프리팹 제거) |
| `9157190` | ExchangeIcon 재귀 검색 + 위치 조정 (TopArea 겹침 해소) |
| `c626605` | **CurrencyHeader → BettingPanel** 자식으로 통합 (단일 사용처 별도 프리팹 제거) |

→ 결과: 베팅 UI 관련 요소가 BettingPanel.prefab 안으로 집중. 별도 프리팹 2개 삭제.

### Phase 2 마무리 — UC9/UC10 라벨 한글화
| 커밋 | 내용 |
|------|------|
| `6418e28` | StringTable.csv 라벨 7언어 갱신: `str.hud.total_score` "총점" → "도파민 스톤", `str.ui.betting.points_label` "획득 포인트" → "획득 도파민 스톤" |

→ WalletManager API 시뮬레이션 7 시나리오 PASS (ResetForNewGame / TryBet / Reward / RollExchangeRate / 보유초과차단 / 0차단 / ShouldGameOver). Phase 2 게이트 통과.

### Editor 시각 + RectTransform 정상화
| 커밋 | 내용 |
|------|------|
| `a0f7c46` | BettingPanel/CharacterInfoPopup prefab 기본 `active=false` (Prefab Editor 시각 정리, 런타임 동작 영향 0) |
| `a1b0eed` | BettingPanel/TrackInfoPanel RectTransform 정상화 — anchor 우측 중앙 단일 앤커 (음수 sizeDelta 제거) |
| `a08ee3d` | BettingPanel/ExchangeIcon을 TrackInfoPanel 바로 아래로 재배치 (START 버튼 옆 → 우측 패널 하단) |

### CharacterInfoPopup 스타일 통일
| 커밋 | 내용 |
|------|------|
| `e1d19f2` | BetAmountModal 배경을 CharacterInfoPopup과 동일한 9-slice 프레임 (Img_BG_ListSlot_BG_01 + Type=9-Slice + 흰색)으로 통일 |
| `f8b744a` | ResultPanel/RankSection 배경도 동일한 9-slice 프레임으로 통일 |

### ResultPanel 미세 조정
| 커밋 | 내용 |
|------|------|
| `fd63f22` | COLOR_RESULT_LOSE `(0.7, 0.7, 0.7)` 밝은 회색 → `(0.4, 0.4, 0.4)` 어두운 회색 + ScoreFormulaText 폰트 32→36 |
| `b2de55b` | COLOR_RESULT_LOSE `(0.4)` → `(0.55)` 너무 어두워 중간값으로 재조정 |

### 도트 패턴 장식
| 커밋 | 내용 |
|------|------|
| `ffbc509` | ResultPanel/RankSection (20개) + BetAmountModalPrefab/Modal (9개) 하단에 Img_Rank 도트 패턴 장식 추가 (캐릭터 카드 스타일) |

---

## SPEC-028 진행 상황

| Phase | 상태 |
|-------|------|
| **Phase 1** — 통화 시스템 + 1.1 클램프 | ✅ 완료 (tag `spec028-phase1-passed` @ `775e218`) |
| **Phase 2** — 베팅액 모달 + 통화 헤더 + 환전 시스템 | ✅ 완료 (tag `spec028-phase2-passed`) |
| **Phase 3** — GAME OVER + Finish 화면 | ✅ 완료 (tag `spec028-phase3-passed` @ `b85f046`) |
| **Phase 4** — UGS 통합 + 닉네임 모달 | ⏸ 미착수 (오너 사전 작업 필요: UGS Dashboard 프로젝트 + Leaderboard ID 발급) |
| **Phase 5** — 글로벌 랭킹 뷰 | ⏸ 미착수 (Phase 4 의존) |

---

## 부수 산출물 (Docs/plans/)
- `prefab_folder_정리_20260523.md`
- `currencyheader_to_bettingpanel_merge_20260525.md`
- `trackinfo_rect_cleanup_20260525.md`
- `betamount_bg_sync_20260525.md`

각 작업마다 별도 plan 문서로 분리 + 롤백 태그 부착 워크플로우 정착 ("맥락별 관리" 원칙).

---

## 학습한 패턴

### 안전 작업 워크플로우
1. **사전 조사** (Explore agent + MCP 시각 검증)
2. **롤백 태그 부착** (`before-<작업명>`)
3. **별도 plan 문서** 작성 (Docs/plans/)
4. **Editor 일회성 스크립트** (Assets/Editor/) 또는 MCP `SerializedObject` 직접 조작
5. **임시 스크립트 삭제** + atomic commit (push 보류)
6. **Play 모드 검증** → 오너 컨펌 OK → push, NG → 롤백

### Unity Prefab 작업 시 주의점
- MCP context에 `UnityEngine.UI` 어셈블리 없음 → `GetComponent("UnityEngine.UI.Image")` 문자열 방식 또는 Editor 스크립트 파일 사용
- 프리팹 편집은 `PrefabUtility.LoadPrefabContents` + `SaveAsPrefabAsset` 패턴
- `SceneBootstrapper.Spec028UI.cs` 같은 partial은 재귀 검색(`GetComponentsInChildren`)으로 자식 위치 탐색 (부모-자식 관계 변경 대비)

---

## 다음 단계
- **오너 사전 작업** (Phase 4 진입 전): UGS Dashboard 프로젝트 생성 + Leaderboard ID `dopamine_stone_global` 발급
- **Resources/Data/BadwordList.csv** 초기 차단어 리스트 작성 (한국어 ~100단어)
- 위 두 작업 완료 후 → Phase 4 (UGS 통합) 착수
