# 20260517 AutoRaceRunner 배회 버그 수정 + 100게임 자동 테스트 분석

**날짜**: 2026-05-17 | **커밋**: f75ea4d

## 1. 배회 버그 수정

### 증상
- AutoRaceRunnerWindow 실행 시 레이서 `isRacing=True` AND `isWandering=True` 동시 발생
- `TotalProgress ≈ 0.059`에서 고착 → 레이스 무한 정지

### 근본 원인 (3개)

| # | 원인 | 코드 |
|---|------|------|
| 1 | `StartNewGame()→ChangeState(Betting)→ScatterAndWander()` 가 `StartRacing()` 이후 실행되는 타이밍 미스 | `RaceManager.OnGameStateChanged` |
| 2 | AutoRaceRunnerWindow가 Round>1 상태에서 시작 시 `LineUpAndStart()` 우회, `gm.StartRace()` 직접 호출 | `AutoBetAndStart()` |
| 3 | play mode 중 재컴파일 → static `Instance` 필드 리셋 → `Instance==null` 상태로 singleton 불일치 | `GameManager/RaceManager.Instance` |

### 수정

| 파일 | 수정 내용 |
|------|-----------|
| `RacerController.Wander.cs` | `StartWandering()`에 `if (isRacing) return;` 가드 추가 |
| `GameManager.cs` | `OnDestroy()`에 `Instance = null` 추가 |
| `RaceManager.cs` | 동일 싱글턴 리셋 추가 |
| `AutoRaceRunnerWindow.cs` | 항상 `StartNewGame()`으로 클린 초기화, `FindFirstObjectByType` fallback 추가 |

### 검증
플레이모드에서 `Wandering=0 Racing=9 prog[0]=0.529` 확인 — 배회 없이 레이스 정상 진행 ✅

---

## 2. 100게임 자동 테스트 결과 (test_260418173919)

**환경**: V4 | 4라운드(2L/3L/4L/5L) | 100게임 400레이스

### 타입별 승률

| 타입 | 승률 | 평균순위 | Top3% | SpurtHP | EmergAvg |
|------|------|----------|-------|---------|----------|
| 도주(R) | 27.3% | 6.1 | 21% | 17% | 1.9 |
| 선행(L) | 21.5% | 5.9 | 24% | 33% | 3.5 |
| 선입(C) | 28.5% | 3.7 | 50% | 48% | 3.7 |
| 추입(K) | 22.8% | 4.3 | 38% | 46% | 2.1 |

**σ = 2.94 ✅** (≤5 기준 통과)

### 거리별 1위율

| 거리 | 도주 | 선행 | 선입 | 추입 |
|------|------|------|------|------|
| 2L | **53%** ⚠️ | 18% | 16% | 13% |
| 3L | 22% | 28% | 31% | 19% |
| 4L | 20% | 21% | **37%** | 22% |
| 5L | 14% | 19% | 30% | **37%** |

### 주요 발견

| 항목 | 내용 |
|------|------|
| 2L 도주 과강세 | 53% — 기대치 25%의 2배. 단거리에서 HP 드레인 누적 시간 부족이 원인 |
| 침묵(구 고요) 개별 과강세 | w=20 avg=2.6 — 전 스탯 최고치(SPD/STM/BRV/LCK=20) |
| 침묵 제외 시 도주 승률 | 27.3% → 22.2% (−5.1%p). σ: 2.94 → 2.78 |
| Emergency Drain 비대칭 | 도주4.1/선행4.5 vs 선입0.9/추입1.0 — 설정값 확인 필요 |
| 선행 열세 | EmergAvg 3.5로 HP 압박 큼에도 승률 최하 |

### 오너 결정
- 밸런스 현황 파악 수준으로 종결 (SPEC 없이)
- 2L 도주 과강세, 선행 열세 → 향후 전용 밸런스 SPEC 검토 필요
- 침묵(goyo) 캐릭터 스탯 재조정 → 별도 세션

---

## 3. 캐릭터 이름 변경

`str.char.000.name`: **고요 → 침묵** (ko만 변경, charId `char.runner.goyo.000` 유지)
