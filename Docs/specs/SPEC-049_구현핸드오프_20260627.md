# SPEC-049 라이브 포트레이트 — 구현 핸드오프

> 캐릭터 정보창 일러스트(PNG) → mp4 비디오. 원본 plan: `.claude/plans/4-adaptive-meteor.md`
> 이 문서 하나로 새 대화에서 이어갈 수 있도록 진행상황 + 계획 정리.

## 현재 진행 (2026-06-27)

| 단계 | 상태 | 커밋 |
|------|------|------|
| 세이브포인트 | ✅ mp4 8개 커밋 + 태그 `pre-spec049` | `f0f985c` |
| 단계 0 | ✅ feature-director + SPEC-049 명세 + §0 측정 | `c103ba6` |
| **단계 A** | ✅ char_video CSV col20 + 파서 + VideoResourcePath | `541a3f8` |
| **단계 B** | ✅ `CharacterVideoController.cs` 컴파일+기능 검증(GATE-B ①②⑤ PASS, ③④는 단계 D Play로 위임) | `a3497af` |
| **단계 C** | ✅ `CharacterVideoPrefabPatcher` + 생성기 동기 — IllustrationVideo 노드(RawImage+ARFitter, 형제 RectTransform 복사). GATE-C PASS(client 구현+qa 독립검증, 멱등·회귀·누수0) | `3365dde` |
| **단계 D** | ✅ `CharacterInfoPopup` 비디오 통합 — Init 부착·Show 분기·PNG↔비디오 크로스페이드(0.18s)·Hide/OnDisable StopAndRelease·OnDestroy 구독해제. GATE-D 에디트모드 PASS(client+qa). **Play 실재생 5건 owner 수동 대기** | (커밋 예정) |
| 단계 E~G | ⏳ 미착수 | — |

**롤백**: `git reset --hard pre-spec049` (또는 직전 단계 커밋)
**작업 위치**: 메인 디렉터리 `D:/Unity_Project/DopamineRace/Assets/...` 직접 편집 (Unity 메인 실행, MCP 즉시 검증. 워크트리 편집 금지)

## §0 측정 결과 (확정)
8개 mp4 전부 **1:1 정사각**(480²·640²), PNG도 1:1(256²), **Δ종횡비 0%** → 레이아웃 불일치 리스크 해소.
- 레이아웃: `AspectRatioFitter.HeightControlsWidth` 유지
- RT: **640×640**, `filterMode=Point`
- showMaskGraphic: 현행(여백 노출 없음)
- 코덱: H264. fps 16/24/32 혼재(§E에서 24 통일 검토), 길이 ~5초

## 확정 결정
1. 범위: 캐릭터 정보창만, 하이브리드 폴백(비디오 없으면 PNG)
2. Standalone 전용 (WebGL 자동 PNG 폴백)
3. 데이터: `CharacterDB_V4.csv` `char_video` col20 (빈 값이면 `char_illustration` PNG)
4. 영상 크기: BettingPanel 프리팹에서 디자이너 인스펙터 조절 (런타임 RectTransform 미변경)
5. 총괄: `feature-director` 에이전트

## 단계별 상세 (A~G, 각 DEV→GATE)

GATE 5체크: ①컴파일0 ②콘솔 Error/Warning0 ③자동TC100% ④수동Play TC100% ⑤직전회귀PASS. P0 미충족 시 진행 불가.

- **A ✅** CSV char_video + CharacterData/V4 파싱 + VideoResourcePath. (검증: VideoResourcePath 8/8, charIllustration 회귀 0/32)
- **B ✅** `CharacterVideoController.cs`: VideoPlayer→RT640²(Point)→RawImage, 무음(audioOutputMode=None)·루프·waitForFirstFrame·skipOnDrop, charId 토큰 가드, errorReceived→OnFallback, prepare 타임아웃 2s, 크로스페이드 0.18s, RectTransform 미변경.
  - **검증완료(MCP, Awake 리플렉션 호출)**: 컴파일 0, 타입/멤버 시그니처 OK, RawImage+VideoPlayer 부착, RT=640²/Point, audioOutputMode=None·loop·wff·skipOnDrop·playOnAwake=false, IsActive=false·CurrentAspect=0, raw.enabled=false·raycastTarget=false. RT 누수 0(정리 확인). prepare 실재생은 단계 D Play에서.
- **C ✅** `CharacterVideoPrefabPatcher.cs`(신규, `[MenuItem("DopamineRace/Patch IllustrationVideo (SPEC-049)")]`) — TrackInfoPanelDecorator 패턴(LoadPrefabContents→FindDeep→직접 프로퍼티→SaveAsPrefabAsset, try/finally 언로드, 멱등 `Find("IllustrationVideo")!=null` 가드). `IllustrationMask` 자식에 `IllustrationVideo`(RawImage enabled=false·raycastTarget=false + AspectRatioFitter HeightControlsWidth) 추가, **RectTransform 초기값을 형제 Illustration에서 복사**(라이브 디자이너값 anchoredPos=-3.42·sizeDelta=76.23까지 일치 검증 — 하드코딩 아님). + `BettingUIPrefabCreator.cs` 동기 생성(생성기 기본 앵커, 역할차이 주석). CharacterVideoController는 미부착(단계 D). GATE-C: 컴파일0·콘솔0·멱등(count=1)·회귀(Illustration·CharacterInfoPopup:120 경로 무손상)·누수0 — qa 독립검증 PASS.
- **D ✅** CharacterInfoPopup 통합: `Init()` IllustrationVideo 탐색+CharacterVideoController get-or-add+OnPrepared/OnFallback 구독; `Show()` PNG 가시복구 후 비디오 분기(`StopAndRelease`→`TryPlayForCharacter`, OnPrepared→`FadePngOut` PNG alpha 1→0 0.18s 후 enabled=false, OnFallback→`RestorePngVisible`); `Hide()`+`OnDisable()` StopAndRelease(P0 누수가드); `OnDestroy()` 구독해제. charId 전환은 컨트롤러 내부 토큰가드+Show마다 StopAndRelease. 신규 필드 videoController/_pngFadeCo/PNG_FADE_SEC.
  - GATE-D 에디트모드: 컴파일0·콘솔0·Init부착(VC개수1·구독2 target=popup)·신규메서드7 시그니처·회귀(기존 필드11/메서드9·Show 시그니처 불변)·누수0 — qa 독립검증 PASS.
  - **owner Play 수동 대기(5건)**: ①prepare→크로스페이드 실재생 ②charId 전환 연타 ③errorReceived/타임아웃→PNG폴백 ④엔진 OnDestroy/OnDisable RT 실해제 ⑤비디오 활성 시 RawImage 활성+illustration.enabled=false 시각전환. (④⑤는 단계 F 30사이클에서 합산 검증)
- **E** `CharacterVideoTranscoder.cs`(신규, [MenuItem]) — 8개 VideoClipImporter 트랜스코드 ON(H264)+importAudio=false+SaveAndReimport. **MCP 아닌 [MenuItem]/batchmode**(좀비 회피). 1개 워밍→7개 일괄. 검증: "Color primaries 0" 경고 소멸.
- **F** 통합/회귀: 30사이클 Show/Hide 후 RenderTexture/VideoPlayer 카운트 불변, 8비디오+24PNG 전수 클릭, 빌드 후 실제 exe 재생 스모크.
- **G** HIST + `/wiki-ingest` (feature-director 주관).

## P0 체크리스트 (5에이전트 검토)
- [x] 종횡비 §0 선측정 (정사각 1:1 확인)
- [ ] charId 토큰 가드 (B 구현됨, D에서 실검증)
- [ ] errorReceived→PNG 폴백 (Media Foundation 부재 대비, B 구현됨)
- [ ] OnDisable StopAndRelease (정보창 열린 채 씬 전환 누수, D)
- [ ] illustration.enabled=false (비디오 활성 시, D)
- [ ] 크로스페이드 150~200ms (B 0.18s, D에서 PNG 페이드아웃 연결)
- [ ] 트랜스코드 [MenuItem]/batchmode (E)
- [ ] 30사이클 누수 검증 (F)
- [ ] 앵커: 라이브 프리팹 RectTransform 복사 (C, 하드코딩 금지)

## 신규/수정 파일
- 신규: `Assets/Scripts/Manager/UI/CharacterVideoController.cs`(✅), `Assets/Scripts/Editor/CharacterVideoPrefabPatcher.cs`(C), `Assets/Scripts/Editor/CharacterVideoTranscoder.cs`(E), `.claude/agents/feature-director.md`(✅)
- 수정: `CharacterData.cs`(✅), `CharacterDataV4.cs`(✅), `CharacterDB_V4.csv`(✅), `CharacterInfoPopup.cs`(D), `BettingUIPrefabCreator.cs`(C), `DopamineBuildVerify.cs`(P1)

## 새 대화 시작 프롬프트
> "SPEC-049 라이브 포트레이트 이어서 진행. 핸드오프: `Docs/specs/SPEC-049_구현핸드오프_20260627.md`, 원본 plan: `.claude/plans/4-adaptive-meteor.md`. 단계 A까지 커밋(`541a3f8`) 완료, 단계 B의 CharacterVideoController.cs 작성됨(미커밋). 단계 B 컴파일 검증부터 진행."

## 알려진 이슈
- 긴 대화에서 도구 호출 여는 태그가 간헐적으로 깨짐(모델 출력) → 새 대화 권장.
