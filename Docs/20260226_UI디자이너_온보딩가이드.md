# DopamineRace — UI 디자이너 온보딩 가이드

**작성일**: 2026-02-26
**대상**: UI 디자인 담당자 (비개발자 / Unity 초보자 포함)
**프로젝트**: DopamineRace (2D 경마 배팅 게임)

---

## 목차

1. [게임 개요](#1-게임-개요)
2. [환경 설정 (Unity 설치)](#2-환경-설정)
3. [Git & GitHub 워크플로우](#3-git--github-워크플로우)
4. [프로젝트 구조](#4-프로젝트-구조)
5. [씬 구성](#5-씬-구성)
6. [UI 프리팹 시스템](#6-ui-프리팹-시스템)
7. [화면별 UI 구조 상세](#7-화면별-ui-구조-상세)
8. [리소스 & 에셋 관리](#8-리소스--에셋-관리)
9. [다국어 시스템](#9-다국어-시스템)
10. [디자인 작업 가이드라인](#10-디자인-작업-가이드라인)
11. [실행 & 테스트 방법](#11-실행--테스트-방법)
12. [주의사항 & FAQ](#12-주의사항--faq)

---

## 1. 게임 개요

### DopamineRace란?

도파민레이스는 **2D 픽셀아트 경마 배팅 게임**입니다.
플레이어는 12마리 캐릭터 중 원하는 캐릭터에 배팅하고, 자동으로 진행되는 레이스를 관전합니다.

### 게임 흐름

```
[타이틀 화면] → [배팅 화면] → [카운트다운] → [레이스 진행] → [결과 화면] → (반복 7라운드) → [최종 정산]
```

### 캐릭터 타입 (4종)

| 타입 | 한국어 | 특징 | 색상 |
|------|--------|------|------|
| Runner | 도주마 | 초반 선두, 후반 체력 고갈 | 🟢 초록 |
| Leader | 선행마 | 중위권 유지, 안정적 | 🔴 빨강 |
| Chaser | 선입마 | 중후반부터 추격 | 🔵 파랑 |
| Reckoner | 추입마 | 후반 폭발, 막판 역전 | 🟣 보라 |

### 배팅 타입 (6종)

| 타입 | 한국어 | 설명 |
|------|--------|------|
| Win | 단승 | 1등 맞추기 |
| Place | 연승 | 1~3등 중 1마리 맞추기 |
| Quinella | 쌍승 | 1~2등 순서 상관없이 맞추기 |
| Exacta | 쌍연 | 1~2등 순서 맞추기 |
| Wide | 삼연 | 1~3등 중 2마리 맞추기 |
| Trio | 광연 | 1~3등 순서 상관없이 맞추기 |

### 해상도 & 아트 스타일

- **참조 해상도**: 1920 × 1080 (16:9)
- **아트 스타일**: 픽셀아트 (12px 기준 폰트)
- **카메라**: Orthographic (2D 전용)

---

## 2. 환경 설정

### 2-1. Unity 설치

#### Unity Hub 설치
1. [Unity 다운로드 페이지](https://unity.com/download) 접속
2. **Unity Hub** 다운로드 및 설치
3. Unity Hub 실행 → Unity 계정 로그인 (없으면 새로 생성)

#### Unity Editor 설치
1. Unity Hub 좌측 **Installs** 클릭
2. 우측 상단 **Install Editor** 클릭
3. **Unity 6000.3.7f1** (Unity 6) 선택하여 설치
   - ⚠️ **반드시 이 버전을 설치해야 합니다** (다른 버전은 호환성 문제 발생)
   - 목록에 없으면 [Unity Archive](https://unity.com/releases/editor/archive)에서 직접 설치
4. 설치 모듈 선택:
   - ✅ **Microsoft Visual Studio** (코드 편집기, 선택사항)
   - ✅ **Windows Build Support** (기본 포함)
   - 나머지는 필요에 따라 선택

### 2-2. 프로젝트 열기

1. Unity Hub → **Projects** 탭
2. **Open** (또는 Add) 클릭
3. 클론한 프로젝트 폴더 `D:\unity\Project\DopamineRace` 선택
4. 첫 실행 시 Import에 시간이 걸림 (5~15분)
5. Console 창에 에러가 없으면 정상

---

## 3. Git & GitHub 워크플로우

### 3-1. Git 설치

1. [Git 다운로드](https://git-scm.com/downloads) → Windows 설치
2. 설치 시 기본 옵션 유지 (Next만 클릭)
3. 설치 확인: 터미널(PowerShell 또는 CMD)에서:
   ```
   git --version
   ```

### 3-2. GitHub 계정 & 접근 권한

1. [GitHub](https://github.com) 계정 생성 (이미 있으면 건너뛰기)
2. 프로젝트 오너(LeeJuls)에게 **GitHub 사용자명**을 알려줄 것
3. 오너가 `https://github.com/LeeJuls/DopamineRace` 저장소에 Collaborator로 추가해줌
4. 이메일로 온 초대 수락

### 3-3. 저장소 클론 (최초 1회)

터미널을 열고:
```bash
# 원하는 폴더로 이동
cd D:\unity\Project

# 저장소 클론
git clone https://github.com/LeeJuls/DopamineRace.git
```

처음 실행 시 GitHub 로그인 팝업이 뜸 → 브라우저에서 로그인

### 3-4. 일상적인 Git 작업 흐름

#### 작업 시작 전 — 최신 코드 받기
```bash
cd D:\unity\Project\DopamineRace
git pull origin main
```

#### 작업 완료 후 — 변경사항 올리기
```bash
# 1. 변경된 파일 확인
git status

# 2. 변경 파일 스테이징
git add Assets/Prefabs/UI/BettingPanel.prefab
git add Assets/Resources/Icon/새파일.png
# 또는 모든 변경사항:
git add .

# 3. 커밋 (저장 단위 생성)
git commit -m "[UI] 배팅 패널 디자인 업데이트"

# 4. 푸시 (GitHub에 업로드)
git push origin main
```

#### 커밋 메시지 규칙
- UI 작업: `[UI] 설명` (예: `[UI] 배팅 패널 색상 변경`)
- 리소스: `[ART] 설명` (예: `[ART] 캐릭터 아이콘 12개 추가`)
- 코드: `[C] 설명` (개발자 전용)

#### 충돌(Conflict) 발생 시
```bash
# 다른 사람과 같은 파일을 수정한 경우
git pull origin main
# 충돌이 발생하면 → 개발자에게 알려주세요!
# .prefab이나 .asset 파일 충돌은 수동 해결이 어려움
```

### 3-5. 추천 Git GUI 도구

터미널이 불편하면 다음 도구를 사용:
- **GitHub Desktop** (가장 쉬움): https://desktop.github.com
- **Sourcetree**: https://www.sourcetreeapp.com
- **Unity 내장 Version Control** 패널 (Window > Version Control)

#### GitHub Desktop 기본 사용법
1. 설치 후 GitHub 계정 로그인
2. File > Clone Repository → URL 탭 → `https://github.com/LeeJuls/DopamineRace.git`
3. 파일 변경 시 자동으로 Changes 탭에 표시
4. Summary에 메시지 입력 → **Commit to main** → **Push origin**

---

## 4. 프로젝트 구조

### 4-1. 전체 폴더 구조

```
DopamineRace/
├── Assets/                          ← ★ 모든 작업은 여기서!
│   ├── Scenes/                      ← 씬 파일 (게임 화면)
│   │   ├── TitleScene.unity         ← 타이틀 화면
│   │   └── SampleScene.unity        ← 메인 게임 (배팅→레이스→결과)
│   │
│   ├── Prefabs/                     ← ★ UI 프리팹 (디자이너 주요 작업 영역)
│   │   └── UI/
│   │       ├── BettingPanel.prefab  ← 배팅 화면 전체 레이아웃
│   │       └── CharacterItem.prefab ← 캐릭터 1행 (목록 아이템)
│   │
│   ├── Resources/                   ← 런타임 로드 리소스
│   │   ├── Data/                    ← CSV 데이터 파일
│   │   │   ├── CharacterDB.csv      ← 캐릭터 정보 (이름, 스탯, 타입)
│   │   │   ├── StringTable.csv      ← 다국어 텍스트 (한/영/일 등 7개국어)
│   │   │   └── TrackDB.csv          ← 트랙 정보
│   │   ├── BG/                      ← 배경 이미지
│   │   │   ├── main_title.png       ← 타이틀 로고
│   │   │   └── main_title_bg.png    ← 타이틀 배경
│   │   ├── Icon/                    ← 아이콘 에셋
│   │   │   ├── Char/               ← 캐릭터 아이콘 (Character_0~11.png)
│   │   │   ├── sword.png           ← 스킬 아이콘
│   │   │   └── book_icon.png       ← 스토리 아이콘
│   │   ├── Tracks/                  ← 트랙 배경 이미지 (5종)
│   │   ├── TrackPrefabs/            ← 트랙 프리팹 (5종)
│   │   └── GameSettings.asset       ← ★ 게임 마스터 설정 파일
│   │
│   ├── Fonts/                       ← 폰트 파일
│   │   ├── ArkPixel/               ← 메인 픽셀 폰트 (12px)
│   │   │   ├── ark-pixel-12px-monospaced-ko.ttf    ← 한국어
│   │   │   └── ark-pixel-12px-monospaced-latin.ttf ← 영문
│   │   └── NeoDunggeunmo/          ← 보조 한글 폰트
│   │       └── neodgm.ttf
│   │
│   ├── Scripts/                     ← C# 스크립트 (개발자 영역)
│   │   ├── Manager/                 ← 게임 매니저들
│   │   │   └── UI/                  ← ★ UI 관련 스크립트
│   │   ├── Data/                    ← 데이터 모델
│   │   ├── Racer/                   ← 레이스 시뮬레이션
│   │   ├── Utility/                 ← 유틸리티 (로컬라이제이션 등)
│   │   ├── Editor/                  ← 에디터 전용 도구
│   │   └── Debug/                   ← 디버그 오버레이
│   │
│   ├── Pixem/                       ← 캐릭터 에디터 (픽셀아트 도구)
│   │   └── Resources/CharacterPrefabs/  ← 캐릭터 프리팹 (12마리 + 어택)
│   │
│   ├── EasyChart/                   ← 차트 라이브러리 (순위 그래프)
│   └── UI Toolkit/                  ← Unity UI 테마 설정
│
├── Docs/                            ← 프로젝트 문서
│   ├── specs/                       ← 기능 명세서
│   ├── history/                     ← 작업 히스토리
│   └── setup/                       ← 설정 가이드
│
├── Packages/                        ← Unity 패키지 (자동 관리, 수정 금지)
└── ProjectSettings/                 ← Unity 프로젝트 설정 (자동 관리)
```

### 4-2. 디자이너가 주로 작업하는 폴더

| 폴더 | 작업 내용 |
|------|----------|
| `Assets/Prefabs/UI/` | UI 프리팹 수정 (레이아웃, 컴포넌트 배치) |
| `Assets/Resources/BG/` | 배경 이미지 교체/추가 |
| `Assets/Resources/Icon/` | 아이콘 교체/추가 |
| `Assets/Resources/Icon/Char/` | 캐릭터 아이콘 교체 |
| `Assets/Fonts/` | 폰트 교체/추가 |
| `Assets/Resources/Tracks/` | 트랙 배경 이미지 교체 |

---

## 5. 씬 구성

### 5-1. TitleScene (빌드 인덱스 0)

타이틀 화면입니다. 게임을 시작하면 처음 보이는 화면.

**구성 요소:**
- 배경 이미지 (`main_title_bg.png`)
- 로고 (`main_title.png`)
- "Press to Start" 깜박이는 텍스트
- 언어 선택 드롭다운 (7개 언어)
- 캐릭터 러너 (12마리가 좌→우로 순차 달림)
- BGM

**관련 스크립트:** `TitleSceneManager.cs`, `TitleCharacterRunner.cs`

### 5-2. SampleScene (빌드 인덱스 1)

메인 게임 루프가 돌아가는 씬. 5개 상태(State)로 전환됩니다.

| 상태 | 화면 | 설명 |
|------|------|------|
| Betting | 배팅 화면 | 캐릭터 목록 + 배팅 타입 선택 |
| Countdown | 카운트다운 | 3-2-1 카운트 |
| Racing | 레이스 | 실시간 달리기 + 순위 HUD |
| Result | 결과 | 라운드 결과 + 배당 정산 |
| Finish | 최종 정산 | 7라운드 종합 결과 |

**관련 스크립트:** `SceneBootstrapper.cs` (+ 6개 partial 파일)

### 5-3. 씬 전환 효과

`SceneTransitionManager.cs`가 16×10 블록 디졸브/빌드업 효과를 담당합니다.
DontDestroyOnLoad로 씬 간 유지됩니다.

---

## 6. UI 프리팹 시스템

### 6-1. 프리팹이란?

Unity에서 **프리팹(Prefab)** 은 재사용 가능한 GameObject 템플릿입니다.
UI 요소들을 프리팹으로 만들어두면 코드에서 필요할 때 복제(Instantiate)하여 사용합니다.

### 6-2. 현재 UI 프리팹 목록

| 프리팹 | 경로 | 용도 |
|--------|------|------|
| **BettingPanel** | `Assets/Prefabs/UI/BettingPanel.prefab` | 배팅 화면 전체 레이아웃 |
| **CharacterItem** | `Assets/Prefabs/UI/CharacterItem.prefab` | 캐릭터 목록의 1행 아이템 |

### 6-3. 프리팹 생성 방법 (에디터 메뉴)

이 프로젝트의 프리팹은 **코드로 자동 생성**됩니다.

```
Unity 메뉴: DopamineRace > Create Betting UI Prefabs
```

이 메뉴를 실행하면:
1. `CharacterItem.prefab` 생성/갱신
2. `BettingPanel.prefab` 생성/갱신
3. `GameSettings.asset`에 프리팹 참조 자동 연결

> ⚠️ **중요**: 프로젝트를 처음 열거나, 프리팹이 깨졌을 때 이 메뉴를 실행하세요.

### 6-4. 프리팹 수정 방법

#### 방법 A: Prefab Mode에서 수정 (권장)
1. Project 창에서 `Assets/Prefabs/UI/BettingPanel.prefab` 더블클릭
2. Prefab Mode로 진입 (씬과 분리된 편집 화면)
3. Hierarchy에서 원하는 요소 선택 → Inspector에서 수정
4. 좌측 상단 `<` 버튼으로 빠져나감 (자동 저장)

#### 방법 B: 씬에서 수정 후 Apply
1. Play 모드가 아닌 상태에서 프리팹 인스턴스 선택
2. Inspector에서 수정
3. 프리팹 인스턴스 우측 상단 **Overrides** > **Apply All**

> ⚠️ Play 모드(게임 실행 중)에서 수정한 내용은 Play 종료 시 **모두 사라집니다!**

### 6-5. 캔버스(Canvas) 설정

UI는 런타임에 코드가 Canvas를 생성하고 프리팹을 배치합니다.

**Canvas 기본 설정:**
- Render Mode: **Screen Space - Overlay**
- Canvas Scaler:
  - UI Scale Mode: **Scale With Screen Size**
  - Reference Resolution: **1920 × 1080**
  - Match Width Or Height: **0.5** (가로/세로 균등 배분)
- Pixel Perfect: **ON**

---

## 7. 화면별 UI 구조 상세

### 7-1. 배팅 화면 (BettingPanel)

배팅 화면은 게임의 핵심 UI입니다. 아래는 전체 레이아웃 구조입니다.

```
┌─────────────────────────────────────────────────────────────┐
│ TopArea (상단 32%)                                           │
│ ┌──────────────────────────────────────────────────────────┐│
│ │ RoundText  │  TitleText ("배팅하기")                      ││
│ │────────────────────────────────────────────────────────── ││
│ │ [단승] [연승] [쌍승] [쌍연] [삼연] [광연]   ← 탭 6개     ││
│ │────────────────────────────────────────────────────────── ││
│ │ BetDescText (배팅 설명)  │  배당률 × 포인트              ││
│ └──────────────────────────────────────────────────────────┘│
├────────────────────────────────────┬────────────────────────┤
│ CharacterListPanel (좌측 60%)      │ (우측 여백/정보 영역)   │
│ ┌────────────────────────────────┐ │                        │
│ │ [✓ 정보 안보기] HideInfoToggle │ │                        │
│ │ ┌────────────────────────────┐ │ │                        │
│ │ │ CharacterItem #1           │ │ │  CharacterInfoPopup    │
│ │ │ CharacterItem #2           │ │ │  (캐릭터 클릭 시 팝업)  │
│ │ │ CharacterItem #3           │ │ │  (우측 50% 슬라이드인)  │
│ │ │ ...                        │ │ │                        │
│ │ │ CharacterItem #12          │ │ │                        │
│ │ └────────────────────────────┘ │ │                        │
│ └────────────────────────────────┘ │                        │
├────────────────────────────────────┴────────────────────────┤
│ TrackInfoPanel (하단 11%)                                    │
│ │ 총 라운드 │ 트랙이름 │ 거리(바퀴수) │ 트랙타입 │          │
│                                          │ [경기 시작] 버튼  │
└─────────────────────────────────────────────────────────────┘
```

### 7-2. CharacterItem (캐릭터 1행)

```
┌──────────────────────────────────────────────────────────┐
│ [아이콘]  인기1  번개   5전 2승   2착: 1회    [1착]      │
│  64×64   순위   이름   전적      2착횟수    착순라벨     │
│  + 컨디션                                    (배팅 시)   │
│    아이콘                                                │
└──────────────────────────────────────────────────────────┘
```

| 요소 | 오브젝트 이름 | 설명 |
|------|-------------|------|
| 캐릭터 아이콘 | `IconContainer/Icon` | 64×64, RectMask2D로 클리핑 |
| 컨디션 아이콘 | `ConditionIcon` | 24×24, 화살표 아이콘 (최상↑↑/상↑/중→/하↓/최하↓↓) |
| 인기순위 | `PopularityLabel` | "인기1", "인기2" 등 |
| 이름 | `NameLabel` | 다국어 대응 (Loc키 기반) |
| 전적 | `RecordLabel` | "N전 M승" |
| 2착 | `SecondLabel` | "2착: N회" |
| 착순라벨 | `BetOrderLabel` | 배팅 선택 시 표시 ("1착", "2착" 등) |
| 배경 | `Background` | 선택 시 색상 변경 |

### 7-3. CharacterInfoPopup (캐릭터 상세 팝업)

캐릭터를 클릭하면 우측에서 슬라이드인되는 상세 정보 창입니다.

```
┌──────────────────────────────────┐
│ Layout1_TopArea (28%)            │
│ ┌──────────────────────────────┐ │
│ │ [타입: 도주마]      [X 닫기] │ │
│ │ "최근 경기기록"              │ │
│ │ 단거리: 3 → 1 → 5           │ │
│ │ 중거리: 2 → 4 → 1           │ │
│ │ 장거리: -                    │ │
│ └──────────────────────────────┘ │
│                                  │
│ Layout2 (57%)                    │
│ ┌────────────┬─────────────────┐ │
│ │ Left (38%) │ Right (62%)     │ │
│ │ [일러스트]  │ [레이더차트]    │ │
│ │            │  속도/힘/용기   │ │
│ │ 승률: 30%  │  침착/지구/행운 │ │
│ │            │                 │ │
│ │ [📖]      │                 │ │
│ └────────────┴─────────────────┘ │
│                                  │
│ Layout3_Bottom (13%)             │
│ ┌──────────────────────────────┐ │
│ │ ⚔️ 스킬이름: 설명 텍스트    │ │
│ └──────────────────────────────┘ │
└──────────────────────────────────┘
```

### 7-4. 레이스 HUD

레이스 진행 중 표시되는 정보:
- **좌상단**: 라운드/바퀴 텍스트 ("라운드 3 — 2/4바퀴")
- **우상단**: 스코어 ("보유: 1,500pt")
- **좌측 순위판**: 실시간 1~8위 순위 (캐릭터 이름 + 색상)
- **하단 타이머**: 경과 시간

### 7-5. 결과 화면

라운드 종료 후 표시:
- 1~3등 캐릭터 + 배당률
- 배팅 결과 (적중 여부)
- 획득/손실 포인트
- "다음 라운드" 버튼

### 7-6. 최종 정산 화면

7라운드 종료 후:
- 총 획득 포인트
- 라운드별 배팅 기록
- 리더보드

---

## 8. 리소스 & 에셋 관리

### 8-1. 이미지 에셋 목록

#### 배경 이미지 (`Assets/Resources/BG/`)
| 파일 | 용도 | 현재 크기 |
|------|------|----------|
| `main_title.png` | 타이틀 로고 | 픽셀아트 |
| `main_title_bg.png` | 타이틀 배경 | 전체화면 |

#### 트랙 배경 (`Assets/Resources/Tracks/`)
| 파일 | 트랙 |
|------|------|
| `bg_normal.png` | 기본 트랙 |
| `bg_rainy.png` | 비 트랙 |
| `bg_desert.png` | 사막 트랙 |
| `bg_highland.png` | 고원 트랙 |
| `bg_snow.png` | 눈 트랙 |

#### 캐릭터 아이콘 (`Assets/Resources/Icon/Char/`)
- `Character_0.png` ~ `Character_11.png` (12마리)
- 배팅 화면 캐릭터 목록에 사용

#### UI 아이콘 (`Assets/Resources/Icon/`)
| 파일 | 용도 |
|------|------|
| `sword.png` | 스킬 아이콘 (캐릭터 정보창) |
| `book_icon.png` | 스토리 아이콘 (미사용, 예약) |

### 8-2. 캐릭터 프리팹 (`Assets/Pixem/Resources/CharacterPrefabs/`)

| 프리팹 | 설명 |
|--------|------|
| `Character_12.prefab` ~ `Character_25.prefab` | 캐릭터 스프라이트 (12마리, Pixem 에디터 제작) |
| `Character_12_Attack.prefab` ~ `Character_23_Attack.prefab` | 어택 모션 프리팹 |

> Pixem은 프로젝트 내장 픽셀아트 캐릭터 에디터입니다.

### 8-3. 폰트 시스템

| 폰트 | 파일 | 용도 |
|-------|------|------|
| **ArkPixel** | `ark-pixel-12px-monospaced-ko.ttf` | 메인 UI 폰트 (한국어) |
| **ArkPixel Latin** | `ark-pixel-12px-monospaced-latin.ttf` | 영문/숫자 |
| **NeoDunggeunmo** | `neodgm.ttf` | 보조 한글 폰트 |

폰트는 `GameSettings.asset`의 `mainFont` 필드에서 지정합니다.

### 8-4. 이미지 추가/교체 시 주의사항

1. **Import Settings 확인** (Inspector):
   - Texture Type: **Sprite (2D and UI)**
   - Sprite Mode: **Single**
   - Pixels Per Unit: **100** (일반) 또는 **70** (타이틀 배경)
   - Filter Mode: **Point (no filter)** ← 픽셀아트 필수!
   - Compression: **None** (픽셀아트는 압축하면 깨짐)

2. **파일 이름 규칙**:
   - 영문 소문자 + 언더스코어: `bg_desert.png`, `character_0.png`
   - 한글/공백/특수문자 금지

3. **아이콘 크기 권장**:
   - 캐릭터 아이콘: 64×64 px
   - 컨디션 아이콘: 24×24 px
   - UI 아이콘: 32×32 px 또는 48×48 px

---

## 9. 다국어 시스템

### 9-1. StringTable.csv

모든 UI 텍스트는 `Assets/Resources/Data/StringTable.csv`에서 관리합니다.

**CSV 형식:**
```
Key,Korean,English,Japanese,Portuguese,Spanish,German,Chinese
str.ui.btn.start,경기 시작,Start Race,レース開始,...
str.bet.type.win,단승,Win,単勝,...
str.char.000.name,번개,Thunder,サンダー,...
```

### 9-2. 키 체계

| 접두사 | 용도 | 예시 |
|--------|------|------|
| `str.ui.*` | UI 일반 텍스트 | `str.ui.btn.start` (경기 시작 버튼) |
| `str.bet.*` | 배팅 관련 | `str.bet.type.win` (단승) |
| `str.hud.*` | 레이스 HUD | `str.hud.timer` (타이머) |
| `str.char.*` | 캐릭터 | `str.char.000.name` (번개) |
| `str.track.*` | 트랙 | `str.track.normal` (기본 트랙) |
| `str.ui.condition.*` | 컨디션 | `str.ui.condition.best` (최상) |

### 9-3. 새 텍스트 추가 시

1. `StringTable.csv`를 텍스트 에디터(VS Code 등)로 열기
2. 새 행 추가: `str.ui.새키,한국어,English,日本語,...`
3. 코드에서: `Loc.Get("str.ui.새키")` 로 사용

> ⚠️ CSV 편집 시 **UTF-8 (BOM 없음)** 인코딩으로 저장!

---

## 10. 디자인 작업 가이드라인

### 10-1. RectTransform & 앵커 시스템

Unity UI의 핵심은 **앵커(Anchor)** 입니다.

```
앵커 = 부모의 어디에 고정할 것인가?

┌─────────────────────────┐
│ (0,1)          (1,1)    │  ← 앵커 좌표 (0~1)
│                         │
│     UI 요소 배치         │
│                         │
│ (0,0)          (1,0)    │
└─────────────────────────┘
```

#### 자주 쓰는 앵커 프리셋

| 패턴 | anchorMin | anchorMax | 설명 |
|------|-----------|-----------|------|
| **전체 늘리기** | (0, 0) | (1, 1) | 부모 크기에 맞춤 |
| **좌상단 고정** | (0, 1) | (0, 1) | sizeDelta로 크기 지정 |
| **우하단 고정** | (1, 0) | (1, 0) | 우측 하단에 붙임 |
| **가운데 고정** | (0.5, 0.5) | (0.5, 0.5) | 화면 정중앙 |
| **상단 가로 늘리기** | (0, 1) | (1, 1) | 너비는 부모 따라감, 높이 고정 |

### 10-2. 컬러 팔레트 (현재 사용 중)

| 용도 | 색상 | Hex | RGB |
|------|------|-----|-----|
| 기본 배경 | 짙은 남색 | `#262633` | (38, 38, 51) |
| 선택 배경 | 중간 파랑 | `#404D80` | (64, 77, 128) |
| 강조 (노랑) | 골드 옐로우 | `#FFD933` | (255, 217, 51) |
| 기본 텍스트 | 흰색 | `#FFFFFF` | (255, 255, 255) |
| 보조 텍스트 | 밝은 회색 | `#999999`~`#CCCCCC` | (153~204) |
| 탭 비활성 | 어두운 남색 | `#33334D` | (51, 51, 77) |
| 탭 활성 | 밝은 파랑 | `#4D66B3` | (77, 102, 179) |

#### 캐릭터 타입 색상
| 타입 | 색상 |
|------|------|
| Runner (도주) | 초록 |
| Leader (선행) | 빨강 |
| Chaser (선입) | 파랑 |
| Reckoner (추입) | 보라 |

#### 컨디션 색상
| 컨디션 | 색상 | Hex |
|--------|------|-----|
| 최상 | 보라 | `#FF44FF` |
| 상 | 초록 | `#66FF66` |
| 중 | 흰색 | `#FFFFFF` |
| 하 | 주황 | `#FFAA44` |
| 최하 | 빨강 | `#FF4444` |

### 10-3. 디자인 적용 워크플로우

UI 디자인을 프로젝트에 적용하는 단계:

#### Step 1: 디자인 시안 제작
- Figma, Photoshop, 등 원하는 도구로 시안 제작
- 참조 해상도: **1920 × 1080**
- 앵커 포인트를 미리 결정해두면 작업이 수월

#### Step 2: 에셋 추출 & 임포트
1. 개별 UI 요소를 PNG로 내보내기 (투명 배경)
2. `Assets/Resources/` 하위 적절한 폴더에 넣기
3. Unity에서 Import Settings 설정 (10-1 참고)

#### Step 3: 프리팹 수정
1. `Assets/Prefabs/UI/BettingPanel.prefab` 더블클릭 → Prefab Mode
2. 기존 요소의 Image 컴포넌트 → Source Image를 새 스프라이트로 교체
3. RectTransform으로 위치/크기 조정
4. 새 요소 필요 시: 빈 GameObject 추가 → Image/Text 컴포넌트 부착

#### Step 4: 개발자와 연동
- 새 UI 요소를 추가한 경우, **오브젝트 이름**을 개발자에게 전달
- 코드에서 `transform.Find("오브젝트이름")`으로 참조하기 때문
- **기존 오브젝트 이름은 변경하지 마세요!**

### 10-4. 반응형 레이아웃 규칙

```
DO ✅                              DON'T ❌
─────────────────────────────      ─────────────────────────────
앵커로 상대 위치 설정              절대 픽셀 좌표로 배치
전체 늘리기(Stretch) 사용          고정 크기(Fixed Size)만 사용
폰트 BestFit 켜기                 폰트 크기 하드코딩
1920×1080 기준으로 작업            임의 해상도 기준 작업
```

---

## 11. 실행 & 테스트 방법

### 11-1. 기본 실행

1. Unity에서 `Assets/Scenes/TitleScene.unity` 열기
2. 상단 **▶ Play** 버튼 클릭
3. 타이틀 → 아무 곳 클릭 → 배팅 화면 진입
4. 캐릭터 선택 후 "경기 시작" 클릭 → 레이스 관전

### 11-2. 배팅 화면 직접 테스트

배팅 화면만 빠르게 테스트하려면:
1. `Assets/Scenes/SampleScene.unity` 열기
2. Play → 바로 배팅 화면으로 진입

### 11-3. 프리팹 재생성 (문제 발생 시)

프리팹이 깨지거나 UI가 이상할 때:
```
Unity 메뉴: DopamineRace > Create Betting UI Prefabs
```

### 11-4. 디버그 오버레이

레이스 중 **F1** 키 → 디버그 정보 표시 (배당률, 순위, HP 등)

### 11-5. 해상도 테스트

1. Game 뷰에서 좌측 상단 해상도 드롭다운 클릭
2. 다양한 해상도에서 UI 확인:
   - 1920×1080 (기준)
   - 1280×720
   - 2560×1440

---

## 12. 주의사항 & FAQ

### 12-1. 꼭 지켜야 할 것

| 규칙 | 이유 |
|------|------|
| **기존 오브젝트 이름 변경 금지** | 코드가 이름으로 참조함 |
| **Play 모드에서 수정 금지** | 종료 시 모두 사라짐 |
| **prefab 수정 후 Apply 필수** | 안 하면 씬 인스턴스만 변경 |
| **이미지는 Point Filter + No Compression** | 픽셀아트 선명도 유지 |
| **CSV 파일은 UTF-8 저장** | 한글 깨짐 방지 |
| **git pull 후 작업 시작** | 충돌 방지 |
| **.prefab 충돌 시 개발자에게** | 프리팹은 수동 머지 어려움 |

### 12-2. 자주 묻는 질문

**Q: UI가 안 보여요!**
A: `DopamineRace > Create Betting UI Prefabs` 메뉴 실행 후 Play

**Q: 한글이 깨져요!**
A: Font가 누락됨. GameSettings Inspector에서 mainFont 확인

**Q: 이미지가 흐려요!**
A: Import Settings에서 Filter Mode를 **Point (no filter)** 로 변경

**Q: 프리팹을 수정했는데 게임에 반영이 안 돼요!**
A: Prefab Mode에서 수정했는지 확인. 씬 인스턴스만 수정하면 Apply 필요

**Q: 텍스트가 잘려요!**
A: Text 컴포넌트에서 **Best Fit** 체크, Min/Max Size 설정

**Q: 새 씬을 만들어야 하나요?**
A: 아니요. 기존 두 씬(Title/Sample) 안에서 작업합니다.

**Q: Git에서 .meta 파일이 변경됐다고 나와요!**
A: Unity가 에셋 설정을 .meta에 저장합니다. 함께 커밋하세요.

### 12-3. 도움이 필요할 때

- **Unity 기본 조작**: [Unity Learn](https://learn.unity.com) (무료)
- **Unity UI 튜토리얼**: "Unity UI Tutorial" 검색 (YouTube 추천)
- **Git 사용법**: [GitHub Desktop 가이드](https://docs.github.com/desktop)
- **프로젝트 관련**: 개발자에게 직접 문의

---

## 부록: 파일 참조 맵

### UI 관련 주요 파일

| 카테고리 | 파일 경로 |
|----------|----------|
| 배팅 UI 코드 | `Assets/Scripts/Manager/UI/SceneBootstrapper.Betting.cs` |
| 캐릭터 아이템 | `Assets/Scripts/Manager/UI/CharacterItemUI.cs` |
| 캐릭터 팝업 | `Assets/Scripts/Manager/UI/CharacterInfoPopup.cs` |
| 프리팹 생성기 | `Assets/Scripts/Editor/BettingUIPrefabCreator.cs` |
| 유틸리티 (텍스트 생성 등) | `Assets/Scripts/Manager/UI/SceneBootstrapper.Utilities.cs` |
| 레이스 HUD | `Assets/Scripts/Manager/UI/SceneBootstrapper.Racing.cs` |
| 결과 화면 | `Assets/Scripts/Manager/UI/SceneBootstrapper.Result.cs` |
| 최종 정산 | `Assets/Scripts/Manager/UI/SceneBootstrapper.Finish.cs` |
| 리더보드 | `Assets/Scripts/Manager/UI/SceneBootstrapper.Leaderboard.cs` |
| 타이틀 씬 | `Assets/Scripts/Manager/TitleSceneManager.cs` |
| 게임 설정 | `Assets/Resources/GameSettings.asset` |
| 다국어 CSV | `Assets/Resources/Data/StringTable.csv` |
| 캐릭터 DB | `Assets/Resources/Data/CharacterDB.csv` |

### 프리팹 오브젝트 이름 규약 (코드 참조용)

#### BettingPanel 하위
```
TopArea, RoundText, TitleText, BetTypeTabs,
BetDescText, OddsArea, OddsText, PointsLabel, PointsFormula,
CharacterListPanel, ScrollArea, Content,
TrackInfoPanel, TotalRoundLabel, TrackNameLabel, DistanceLabel, TrackTypeLabel,
HideInfoToggle, CharacterInfoPopup, StartBtn
```

#### CharacterItem 하위
```
Background, IconContainer, Icon, ConditionIcon,
PopularityLabel, NameLabel, RecordLabel, SecondLabel, BetOrderLabel
```

#### CharacterInfoPopup 하위
```
Layout1_TopArea, CharTypeLabel, CloseBtn,
RecentRecordHeader, ShortDistRow, MidDistRow, LongDistRow,
Layout2_Left, Illustration, WinRateBg, WinRateLabel, StoryIconBtn,
Layout2_Right, RadarChartArea,
Layout3_Bottom, SkillIcon, SkillDescLabel
```

---

*이 문서는 DopamineRace 프로젝트의 UI 디자이너 온보딩을 위해 작성되었습니다.*
*문의사항은 개발자에게 직접 연락해주세요.*
